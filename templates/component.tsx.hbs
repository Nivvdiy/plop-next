/**
 * {{pascalCase componentName}} Component
 * 
 * Auto-generated by plop-next at {{now}}
 */

import React, { FC, ReactNode } from 'react';
import styles from './{{pascalCase componentName}}.module.scss';

export interface {{pascalCase componentName}}Props {
  /**
   * The title/label for this component
   * @default "{{pascalCase componentName}}"
   */
  title?: string;

  /**
   * Whether the component is disabled
   * @default false
   */
  disabled?: boolean;

  /**
   * Content to display inside the component
   */
  children?: ReactNode;

  /**
   * Callback when the component is used
   */
  onAction?: () => void;
}

/**
 * {{pascalCase componentName}} - {{description}}
 * 
 * @component
 * @example
 * ```tsx
 * import { {{pascalCase componentName}} } from './{{pascalCase componentName}}';
 * 
 * export function App() {
 *   return (
 *     <{{pascalCase componentName}} 
 *       title="My {{pascalCase componentName}}"
 *       onAction={() => console.log('Action!')}
 *     >
 *       Content goes here
 *     </{{pascalCase componentName}}>
 *   );
 * }
 * ```
 */
export const {{pascalCase componentName}}: FC<{{pascalCase componentName}}Props> = ({
  title = '{{pascalCase componentName}}',
  disabled = false,
  children,
  onAction,
}) => {
  return (
    <div className={styles.container} data-testid="{{kebabCase componentName}}">
      <div className={styles.header}>
        <h2 className={styles.title}>{title}</h2>
      </div>
      <div className={styles.content}>
        {children}
      </div>
      {onAction && (
        <button
          className={styles.button}
          disabled={disabled}
          onClick={onAction}
          data-testid="{{kebabCase componentName}}-action-button"
        >
          Trigger Action
        </button>
      )}
    </div>
  );
};
