/**
 * {{pascalCase utilityName}} Utility
 * 
 * Auto-generated by plop-next
 * Category: {{category}}
 * Description: {{description}}
 */

{{#if includeTypes}}
/**
 * Configuration options for {{camelCase utilityName}}
 */
export interface {{pascalCase utilityName}}Options {
  /**
   * Enable verbose logging
   * @default false
   */
  verbose?: boolean;

  {{#each additionalOptions}}
  /**
   * {{this.description}}
   * @default {{this.default}}
   */
  {{this.name}}?: {{this.type}};

  {{/each}}
}

{{/if}}
/**
 * {{description}}
 * 
 * @example
 * ```ts
 * import { {{camelCase utilityName}} } from './{{kebabCase utilityName}}';
 * 
 * const result = {{camelCase utilityName}}('input');
 * console.log(result);
 * ```
 * 
 * @param input - The input to process
 * @param options - Configuration options
 * @returns The processed result
 */
export function {{camelCase utilityName}}(
  input: {{inputType}},
  {{#if includeTypes}}options?: {{pascalCase utilityName}}Options{{/if}}
): {{returnType}} {
  {{#if includeTypes}}
  const {
    verbose = false,
  } = options ?? {};

  if (verbose) {
    console.log(`[{{pascalCase utilityName}}] Processing:`, input);
  }
  {{/if}}

  // TODO: Implement {{camelCase utilityName}} logic
  // This is a template-generated utility
  
  return input as {{returnType}};
}

{{#if includeAsync}}
/**
 * Async version of {{camelCase utilityName}}
 * 
 * @param input - The input to process
 * @param options - Configuration options
 * @returns Promise of the processed result
 */
export async function {{camelCase utilityName}}Async(
  input: {{inputType}},
  {{#if includeTypes}}options?: {{pascalCase utilityName}}Options{{/if}}
): Promise<{{returnType}}> {
  // Simulate async work
  await new Promise((resolve) => setTimeout(resolve, 0));
  return {{camelCase utilityName}}(input{{#if includeTypes}}, options{{/if}});
}
{{/if}}

{{#if includeValidation}}
/**
 * Validates input for {{camelCase utilityName}}
 */
export function validate{{pascalCase utilityName}}Input(input: unknown): input is {{inputType}} {
  return typeof input === 'string' || typeof input === 'object';
}
{{/if}}
