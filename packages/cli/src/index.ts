// Public API of @plop-next/cli — re-exports everything users need to type their plopfile.
export { Separator } from "@plop-next/core";
export type { PlopNextCore as PlopNext, GeneratorConfig, PlopPrompt, ActionConfig, Action } from "@plop-next/core";
